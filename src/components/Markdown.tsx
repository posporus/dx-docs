/*
import {
    parseMarkdown,
    Component,
} from '../../deps.ts'

class Markdown extends Component {
    constructor(props: { markdown: string }) {
        super(props)
    }
    render() {
        return parseMarkdown(this.props.markdown)
    }
}

export {
    Markdown
}
*/